# Optional engines. Install any subset; the platform auto-detects what is
# present and falls back to the built-in offline implementations otherwise.
# Check what is active at any time via GET /api/system.

# --- Speech recognition -----------------------------------------------------
faster-whisper>=1.0        # recommended: fast CPU/GPU Whisper with timestamps
# openai-whisper           # reference PyTorch implementation

# --- Translation ------------------------------------------------------------
argostranslate>=1.9        # fully offline neural MT (also install language packs)
# or set OPENAI_API_KEY to use LLM translation with length control

# --- Text to speech / cloning ----------------------------------------------
# TTS                      # Coqui XTTS v2 — zero-shot cross-lingual cloning
# f5-tts                   # F5-TTS — clone from 6-10s of reference audio
# chatterbox-tts           # Resemble Chatterbox — MIT, low VRAM
# piper-tts                # fast CPU preset voices (set DUB_PIPER_VOICE_DIR)

# --- Voice conversion -------------------------------------------------------
# rvc-python               # RVC speech-to-speech (set DUB_RVC_MODEL)

# --- Source separation ------------------------------------------------------
# demucs                   # Demucs v4 stem splitting (replaces the HPSS default)

# --- GPU --------------------------------------------------------------------
# torch                    # required by most of the neural engines above
